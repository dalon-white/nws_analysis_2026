# src/

Linear pipeline scripts:
1. `01_load_data.py` – ingest raw data
2. `02_clean_data.py` – clean and transform
3. `03_modeling.py` – fit models and produce risk scores
4. `04_analysis.py` – produce figures/tables

Use `src/sandbox/` for experimental scripts; stable code belongs in `src/`.
